In this project, I built a gradient-boosted regression model (XGBoost) to predict NYC bikeshare trip duration using structured trip-level data, transforming raw operational data into actionable predictive insights. I also engineered temporal and spatial features and optimized model performance through cross-validation and hyperparameter tuning, improving generalization and reducing RMSE relative to a linear regression baseline.
